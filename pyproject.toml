[project]
name = "hrem_evaluation"
version = "0.1.0"
description = "An evaluation framework for HREM models."
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
]

[tool.setuptools]
packages = ["src"]

[tool.black]
line-length = 88
