include "default.json"
  "dataset": {
    "name": "reverse",
    "params": {
      "seq_len": 64,
      "test_size": 1,
      "train_size": 1
    }
  },
  "experiment_name": "baseline_mlp_reverse",
  "model": {
    "name": "mlp",
    "params": {
      "hidden_size": 4,
      "input_size": 64,
      "output_size": 64
    }
  },
  "training": {
    "batch_size": 1,
    "epochs": 1,
    "learning_rate": 0.001
  }