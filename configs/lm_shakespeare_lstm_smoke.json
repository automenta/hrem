{
  "dataset": {
    "name": "tiny_shakespeare",
    "params": {
      "seq_length": 8,
      "split": "train"
    }
  },
  "experiment_name": "lm_shakespeare_lstm_smoke",
  "extends": "default.json",
  "model": {
    "name": "lstm",
    "params": {
      "embedding_dim": 32,
      "hidden_size": 4,
      "num_layers": 2,
      "seq_len": 8
    }
  },
  "training": {
    "batch_size": 1,
    "epochs": 1,
    "grad_clip_norm": 1.0,
    "learning_rate": 0.001
  }
}